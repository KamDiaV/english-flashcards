# English Word Cards (React App)

Интерактивное приложение для изучения английских слов по карточкам. Проект разрабатывается по этапам в рамках учебного блока по React.

---

## 📌 Этап 1: Базовая структура

На этом этапе выполнены:

- Инициализация проекта с использованием Create React App
- Установка и настройка `react-router-dom` для маршрутизации
- Подключение SCSS
- Создание базовых компонентов
- Разметка страниц

---

## 📌 Этап 2: Стилизация и работа с таблицей

На этом этапе выполнено:

1. **SCSS-модули & переменные**  
   - Вынесла все цвета, шрифты и размеры в файл `variables.scss`  
   - Перешла на CSS-модули для компонентов (`*.module.scss`)  
   - Сделала глобальные сбросы и базовые стили в `globals.scss`

2. **Анимации и фидбек интерфейса**  
   - `:hover`, `:focus`, `:active` для кнопок, ссылок и инпутов  

3. **Адаптивная таблица слов**  
   - Условный рендеринг ячеек:  
     - **Просмотр** с кнопками ✏️/🗑  
     - **Редактирование** с `<input>` и кнопками 💾/❌ 


4. **Работа с API и кэширование**  
   - Подключила API:  
     - `GET` — загрузка списка слов  
     - `PUT` — обновление  
     - `DELETE` — удаление  
     - `POST` — добавление  
   - Используем **React Query** для централизованного управления данными и кэш-словарём  
   - Для добавленных записей (поскольку тестовый API их не хранит) сделали **локальное сохранение** в `localStorage`  
   - После `POST` мутации мы через `queryClient.setQueryData` сразу дописываем слово в кэш, а в `localStorage` — в качестве дополнительной «памяти» между перезагрузками  
   - При `DELETE` и `PUT` обновляем одновременно и удаляем/меняем данные в React Query-кэше, и синхронизируем `localStorage`  

---

## 📌 Статус проекта
🔄 Разработка: этап 2 из 8 — стилизация интерфейса, таблица, интеграция с API и локальное хранение.  